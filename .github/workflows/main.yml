on:
  push:
    branches:
      - main
name: 🚀 Deploy website http://qiaraflorist.store/
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}//ftp.iconnect.web.id
        username: ${{ secrets.FTP_USERNAME }}//iconnect@iconnect.web.id
        password: ${{ secrets.FTP_PASSWORD }}//St@nly2020
        server-dir: /qiara-florist/